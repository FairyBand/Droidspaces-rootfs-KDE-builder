name: 编译并发布 Droidspaces RootFS

env:
  ANLAND_REPO: https://github.com/superturtlee/anland.git
  ANLAND_COMMIT: aa1d34727695ee8a75a9b6fb0722753854b80449

on:
  workflow_dispatch: 
    inputs:
      build_target:
        description: '选择要构建的发行版'
        required: true
        type: choice
        default: 'Debian-13-KDE'
        options:
          - 'Debian-13-KDE'
          - 'Ubuntu-24-KDE'
          - 'Ubuntu-25-KDE'
          - 'Ubuntu-26-KDE'
          - 'Ubuntu-26-GNOME-Wayland'
          - 'Fedora-43-KDE'
          - 'Arch-KDE'
          - 'all'
      custom_username:
        description: '自定义用户名（默认 Gold）'
        required: false
        type: string
        default: 'Gold'
      build_KDE:
        description: 'KDE桌面选择(conc-精简 min-最小构造 none-不用KDE)'
        required: true
        type: choice
        default: 'min'
        options: 
          - 'conc'
          - 'min'
          - 'none'
      build_KDE_plus:
        description: 'KDE 桌面开机自启动 (需要 KDE 桌面选择为 conc 或 min)'
        required: true
        type: boolean
        default: false
      PulseAudio:
        description: 'PulseAudio 音频转发'
        required: true
        type: choice
        default: 'socket'
        options: 
          - 'socket'
          - 'tcp'
          - 'none'
      enable_zh_tz:
        description: '使用 中文 语言，并更改时区'
        required: true
        type: boolean
        default: true
      enable_mesa:
        description: '开启 高通骁龙GPU 支持'
        required: true
        type: boolean
        default: true
      enable_tmoe:
        description: '集成 TMOE '
        required: true
        type: boolean
        default: true
      enable_srf:
        description: '开启 输入法 fcitx5 支持'
        required: true
        type: boolean
        default: false
      enable_binfmt:
        description: '开启 跨架构 支持'
        required: true
        type: boolean
        default: false
      enable_yj:
        description: '开启 Nat和硬件识别 支持'
        required: true
        type: boolean
        default: true
      enable_kfgj:
        description: '开发工具 集成'
        required: true
        type: boolean
        default: false
      enable_zip:
        description: '压缩工具 集成'
        required: true
        type: boolean
        default: true
      enable_docker:
        description: 'Docker 集成'
        required: true
        type: boolean
        default: false    

jobs:
  # --------------------------------------------------------
  # 阶段 1: setup (环境准备与变量生成)
  # --------------------------------------------------------
  setup:
    runs-on: ubuntu-latest
    outputs:
      templates: ${{ steps.find-templates.outputs.templates }}
      build_id: ${{ steps.prep.outputs.build_id }}
      build_date: ${{ steps.prep.outputs.build_date }}
    steps:
      - name: 检出代码库
        uses: actions/checkout@v4

      - name: 解析用户的构建选择
        id: find-templates
        run: |
          TARGET="${{ github.event.inputs.build_target }}"
          
          if [ "$TARGET" = "all" ]; then
            TEMPLATES=$(ls *.Dockerfile | sed 's/\.Dockerfile//' | jq -R -s -c 'split("\n")[:-1]')
          else
            TEMPLATES=$(jq -n -c --arg t "$TARGET" '[$t]')
          fi
          
          echo "templates=$TEMPLATES" >> $GITHUB_OUTPUT
          echo "最终决定的构建矩阵: $TEMPLATES"

      - name: 准备构建元数据
        id: prep
        run: |
          BUILD_DATE=$(date +'%Y-%m-%d %H:%M:%S')
          BUILD_ID="v$(date +'%Y%m%d-%H%M%S')"
          echo "BUILD_DATE=$BUILD_DATE" >> $GITHUB_OUTPUT
          echo "BUILD_ID=$BUILD_ID" >> $GITHUB_OUTPUT
          echo "构建 ID: $BUILD_ID"

  # --------------------------------------------------------
  # 阶段 2: build (常规 Linux 发行版 RootFS 的构建)
  # --------------------------------------------------------
  build:
    needs: setup
    if: ${{ needs.setup.outputs.templates != '[]' }}
    runs-on: ubuntu-24.04-arm
    strategy:
      fail-fast: false
      matrix:
        template: ${{ fromJson(needs.setup.outputs.templates) }}
    steps:
      - name: 检出代码库
        uses: actions/checkout@v4

      - name: 验证构建选项
        run: |
          if [ "${{ inputs.build_KDE }}" = "none" ] && [ "${{ inputs.build_KDE_plus }}" = "true" ]; then
            echo "❌ 错误: 当 KDE 桌面选择为 'none' 时，无法启用 KDE 开机自启动"
            echo "请重新运行工作流，取消勾选 'KDE 桌面开机自启动' 或选择 KDE 桌面"
            exit 1
          fi

      - name: 编译 RootFS (${{ matrix.template }})
        run: |
          chmod +x build_rootfs-native.sh
          ./build_rootfs-native.sh \
            -i "${{ matrix.template }}.Dockerfile" \
            -v "${{ needs.setup.outputs.build_id }}" \
            -K "${{ inputs.build_KDE }}" \
            -L "${{ inputs.build_KDE_plus }}" \
            -P "${{ inputs.PulseAudio }}" \
            -g "${{ inputs.enable_zh_tz }}" \
            -a "${{ inputs.enable_binfmt }}" \
            -b "${{ inputs.enable_yj }}" \
            -c "${{ inputs.enable_mesa }}" \
            -d "${{ inputs.enable_kfgj }}" \
            -e "${{ inputs.enable_zip }}" \
            -f "${{ inputs.enable_docker }}" \
            -h "${{ inputs.enable_srf }}" \
            -j "${{ inputs.enable_tmoe }}" \
            -u "${{ inputs.custom_username }}"
        env:
          ANLAND_REPO: ${{ env.ANLAND_REPO }}
          ANLAND_COMMIT: ${{ env.ANLAND_COMMIT }}

      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.template }}-rootfs
          path: "*.tar.xz"
          retention-days: 1

  # --------------------------------------------------------
  # 阶段 3: anland 配套产物 (APK 与 Magisk/KernelSU 模块)
  # --------------------------------------------------------
  anland-artifacts:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - name: 检出 anland
        run: |
          git clone "${ANLAND_REPO}" anland
          cd anland
          git checkout "${ANLAND_COMMIT}"

      - name: 设置 Android SDK
        uses: android-actions/setup-android@v3

      - name: 安装 Android NDK
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake zip
          sdkmanager "ndk;29.0.13113456" "cmake;3.22.1"

      - name: 构建 anland APK 与模块
        run: |
          cd anland
          mkdir -p build_tools
          ln -s "$ANDROID_HOME" build_tools/android-sdk
          rm -f android_consumer/local.properties
          (cd android_consumer && ./gradlew assembleRelease)
          bash build_magisk_module.sh
          mkdir -p ../anland_assets
          cp android_consumer/app/build/outputs/apk/release/app-release.apk ../anland_assets/anland-consumer-${ANLAND_COMMIT}.apk
          cp anland-daemon.zip ../anland_assets/anland-magisk-kernelsu-${ANLAND_COMMIT}.zip
          printf '%s\n' "${ANLAND_COMMIT}" > ../anland_assets/ANLAND_COMMIT.txt

      - name: 上传 anland 配套产物
        uses: actions/upload-artifact@v4
        with:
          name: anland-${{ env.ANLAND_COMMIT }}
          path: anland_assets/*
          retention-days: 1

  # --------------------------------------------------------
  # 阶段 4: release (汇总发布)
  # --------------------------------------------------------
  release:
    needs: [setup, build, anland-artifacts]
    if: ${{ !cancelled() && needs.setup.result == 'success' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 检出代码库
        uses: actions/checkout@v4
        
      - name: 下载所有产物
        uses: actions/download-artifact@v4
        with:
          pattern: "*"
          merge-multiple: true
          
      - name: 整理资源
        run: |
          mkdir -p release_assets
          mv *.tar.xz release_assets/
          mv anland-consumer-*.apk release_assets/
          mv anland-magisk-kernelsu-*.zip release_assets/
          mv ANLAND_COMMIT.txt release_assets/
          PA_CHOICE="${{ github.event.inputs.PulseAudio }}"
          CUSTOM_USERNAME="${{ github.event.inputs.custom_username }}"
          build_KDE_plus="${{ github.event.inputs.build_KDE_plus }}"
          if [ "$PA_CHOICE" != "none" ] && [ "$build_KDE_plus" = "false" ]; then
            cp "scripts/on_aaudio_${PA_CHOICE}.sh" "release_assets/on_aaudio_${PA_CHOICE}.sh"
            sed -i "s/USERNAME=\"Gold\"/USERNAME=\"${CUSTOM_USERNAME}\"/g" "release_assets/on_aaudio_${PA_CHOICE}.sh"
          fi
          
      - name: 创建统一 Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.setup.outputs.build_id }}
          name: "Droidspaces RootFS ${{ needs.setup.outputs.build_id }}"
          files: "release_assets/*"
          body: |
            Droidspaces RootFS 自动构建
            构建时间 : ${{ needs.setup.outputs.build_date }}
            构建目标（用户指定）: ${{ github.event.inputs.build_target }}
            构建 ID: ${{ needs.setup.outputs.build_id }}

            构建选项:
            - **自定义用户名**: ${{ github.event.inputs.custom_username }}
            - **KDE 桌面**: ${{ github.event.inputs.build_KDE }}
            - **KDE 开机自启动**: ${{ github.event.inputs.build_KDE_plus }}
            - PulseAudio 音频转发: ${{ github.event.inputs.PulseAudio }}
            - 使用中文作为主语言: ${{ github.event.inputs.enable_zh_tz }}
            - 高通骁龙 GPU 支持: ${{ github.event.inputs.enable_mesa }}
            - 集成 TMOE : ${{ github.event.inputs.enable_tmoe }}
            - 输入法 fcitx5 支持: ${{ github.event.inputs.enable_srf }}
            - 跨架构支持: ${{ github.event.inputs.enable_binfmt }}
            - 容器硬件与网络识别支持: ${{ github.event.inputs.enable_yj }}
            - 开发工具集成: ${{ github.event.inputs.enable_kfgj }}
            - 压缩工具集成: ${{ github.event.inputs.enable_zip }}
            - Docker 集成: ${{ github.event.inputs.enable_docker }}
            - anland 仓库: ${{ env.ANLAND_REPO }}
            - anland Commit: ${{ env.ANLAND_COMMIT }}
            
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
